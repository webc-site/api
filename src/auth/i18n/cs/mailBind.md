Svázat e-mail: `${=it.to}` (Ověřovací kód e-mailu: `${=it.code}` )

Žádáte o svázání e-mailu účtu:`${=it.to}`

E-mailový ověřovací kód je:

`${=it.token_str}`

Ověřovací kód je platný dvacet čtyři hodin.

Pokud jste nepožádali o svázání e-mailové adresy, ignorujte prosím tento e-mail.
