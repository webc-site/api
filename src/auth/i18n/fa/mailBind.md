پیوند ایمیل: `${=it.to}` (کد تأیید ایمیل: `${=it.code}` )

شما برای اتصال ایمیل حساب درخواست می کنید:`${=it.to}`

کد تایید ایمیل:

`${=it.token_str}`

کد تایید به مدت بیست و چهار ساعت معتبر است.

اگر درخواستی برای اتصال آدرس ایمیل خود نداده اید، لطفاً این ایمیل را نادیده بگیرید.
