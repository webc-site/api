Ändra e-post: `${=it.from}` → `${=it.to}` (Ny e-postverifieringskod: `${=it.code}` )

Du ansöker om att ändra ditt kontos e-postadress: `${=it.from}` → `${=it.to}`

Om detta är din egen operation är den nya e-postverifieringskoden:

`${=it.token_str}`

Verifieringskoden är giltig i tjugofyra timmar.

Om du inte har begärt att ändra din e-postadress, vänligen ignorera detta e-postmeddelande.
