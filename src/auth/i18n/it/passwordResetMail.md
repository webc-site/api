reimpostare la password

Il codice di verifica per la reimpostazione della password è:

`${=it.token_str}`

Il codice di verifica è valido per ventiquattro ore.

Se non hai richiesto la reimpostazione della password, ignora questa email.
