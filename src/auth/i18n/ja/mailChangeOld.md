メールの変更: `${=it.from}` → `${=it.to}` (古いメール確認コード: `${=it.code}` )

アカウントのメール アドレスの変更を申請しています: `${=it.from}` → `${=it.to}`

メール認証コードは次のとおりです。

`${=it.token_str}`

確認コードは 24 時間有効です。

メール アドレスの変更を申請していない場合は、このメールを無視し、アカウントのセキュリティに注意してください。
