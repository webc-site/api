Zviazať e-mail: `${=it.to}` (Verifikačný kód e-mailu: `${=it.code}` )

Žiadate o zviazanie e-mailu účtu:`${=it.to}`

E-mailový overovací kód je:

`${=it.token_str}`

Overovací kód je platný dvadsaťštyri hodín.

Ak ste nepožiadali o viazanie svojej e-mailovej adresy, ignorujte tento e-mail.
