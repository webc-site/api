Ikat email: `${=it.to}` (Kode verifikasi email: `${=it.code}` )

Anda mengajukan permohonan untuk mengikat email akun:`${=it.to}`

Kode verifikasi emailnya adalah:

`${=it.token_str}`

Kode verifikasi berlaku selama dua puluh empat jam.

Jika Anda belum mengajukan permohonan untuk mengikat alamat email Anda, harap abaikan email ini.
