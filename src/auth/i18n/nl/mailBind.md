Bind e-mailadres: `${=it.to}` (E-mailverificatiecode: `${=it.code}` )

Je vraagt ​​je aan om het e-mailadres van je account te binden:`${=it.to}`

De e-mailverificatiecode is:

`${=it.token_str}`

De verificatiecode is vierentwintig uur geldig.

Als u geen aanvraag heeft ingediend om uw e-mailadres te binden, kunt u deze e-mail negeren.
