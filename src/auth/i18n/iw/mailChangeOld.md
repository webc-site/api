שנה דוא"ל: `${=it.from}` → `${=it.to}` (קוד אימות דוא"ל ישן: `${=it.code}` )

אתה מגיש בקשה לשינוי כתובת הדוא"ל של חשבונך: `${=it.from}` → `${=it.to}`

קוד האימות באימייל הוא:

`${=it.token_str}`

קוד האימות תקף לעשרים וארבע שעות.

אם לא הגשת בקשה לשנות את כתובת הדוא"ל שלך, אנא התעלם מאימייל זה ושם לב לאבטחת החשבון.
