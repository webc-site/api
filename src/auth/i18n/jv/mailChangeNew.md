Ngowahi email: `${=it.from}` → `${=it.to}` (Kode verifikasi email anyar: `${=it.code}` )

Sampeyan nglamar kanggo ngowahi alamat email akun: `${=it.from}` → `${=it.to}`

Yen iki operasi sampeyan dhewe, kode verifikasi email anyar yaiku:

`${=it.token_str}`

Kode verifikasi valid kanggo patlikur jam.

Yen sampeyan durung njaluk ngowahi alamat email sampeyan, aja lali email iki.
