Modificar e-mail: `${=it.from}` → `${=it.to}` (código de verificação de e-mail antigo: `${=it.code}` )

Você está solicitando a modificação do endereço de e-mail da sua conta: `${=it.from}` → `${=it.to}`

O código de verificação de e-mail é:

`${=it.token_str}`

O código de verificação é válido por vinte e quatro horas.

Se você não solicitou a modificação do seu endereço de e-mail, ignore este e-mail e preste atenção à segurança da conta.
