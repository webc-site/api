Bind email: `${=it.to}` (Email verification code: `${=it.code}` )

You are applying to bind account email:`${=it.to}`

The email verification code is:

`${=it.token_str}`

The verification code is valid for twenty-four hours.

If you have not applied to bind your email address, please ignore this email.
