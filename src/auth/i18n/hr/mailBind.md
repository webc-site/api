Vezati e-poštu: `${=it.to}` (Kod za provjeru e-pošte: `${=it.code}` )

Prijavljujete se za povezivanje e-pošte računa:`${=it.to}`

Kod za provjeru e-pošte je:

`${=it.token_str}`

Kontrolni kod vrijedi dvadeset i četiri sata.

Ako se niste prijavili za uvezivanje svoje adrese e-pošte, zanemarite ovu e-poruku.
