이메일 수정: `${=it.from}` → `${=it.to}` (기존 이메일 확인 코드: `${=it.code}` )

계정 이메일 주소 수정을 신청 중입니다: `${=it.from}` → `${=it.to}`

이메일 확인 코드는 다음과 같습니다.

`${=it.token_str}`

인증 코드는 24시간 동안 유효합니다.

이메일 주소 수정을 신청하지 않으셨다면 본 이메일을 무시하시고 계정 보안에 유의하시기 바랍니다.
