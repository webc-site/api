이메일 수정: `${=it.from}` → `${=it.to}` (새 이메일 확인 코드: `${=it.code}` )

계정 이메일 주소 수정을 신청 중입니다: `${=it.from}` → `${=it.to}`

본인의 작업인 경우 새 이메일 확인 코드는 다음과 같습니다.

`${=it.token_str}`

인증 코드는 24시간 동안 유효합니다.

이메일 주소 수정을 요청하지 않으셨다면 이 이메일을 무시하시기 바랍니다.
