ای میل کا پابند کریں: `${=it.to}` (ای میل توثیقی کوڈ: `${=it.code}` )

آپ اکاؤنٹ ای میل کو پابند کرنے کے لیے درخواست دے رہے ہیں:`${=it.to}`

ای میل تصدیقی کوڈ یہ ہے:

`${=it.token_str}`

تصدیقی کوڈ چوبیس گھنٹے کے لیے درست ہے۔

اگر آپ نے اپنے ای میل ایڈریس کو پابند کرنے کے لیے درخواست نہیں دی ہے، تو براہ کرم اس ای میل کو نظر انداز کریں۔
