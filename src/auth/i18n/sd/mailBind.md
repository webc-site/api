پابند اي ميل: `${=it.to}` (اي ميل تصديق ڪوڊ: `${=it.code}` )

توھان درخواست ڪري رھيا آھيو بائنڊ اڪائونٽ اي ميل:`${=it.to}`

اي ميل جي تصديق جو ڪوڊ آهي:

`${=it.token_str}`

تصديقي ڪوڊ چوويهن ڪلاڪن لاءِ صحيح آهي.

جيڪڏھن توھان پنھنجي اي ميل پتي کي پابند ڪرڻ لاءِ درخواست نه ڏني آھي، مھرباني ڪري ھن اي ميل کي نظرانداز ڪريو.
