اي ميل تبديل ڪريو: `${=it.from}` → `${=it.to}` (نئون اي ميل تصديق ڪوڊ: `${=it.code}` )

توھان درخواست ڪري رھيا آھيو پنھنجي کاتي جي اي ميل پتي کي تبديل ڪرڻ لاءِ: `${=it.from}` → `${=it.to}`

جيڪڏھن اھو توھان جو پنھنجو عمل آھي، نئون اي ميل تصديقي ڪوڊ آھي:

`${=it.token_str}`

تصديقي ڪوڊ چوويهن ڪلاڪن لاءِ صحيح آهي.

جيڪڏهن توهان پنهنجي اي ميل پتي کي تبديل ڪرڻ جي درخواست نه ڪئي آهي، مهرباني ڪري هن اي ميل کي نظر انداز ڪريو.
