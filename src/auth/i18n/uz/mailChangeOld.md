Elektron pochta manzilini o‘zgartirish: `${=it.from}` → `${=it.to}` (E-pochta manzilini tasdiqlash kodi: `${=it.code}` )

Hisobingiz e-pochta manzilini oʻzgartirish uchun ariza topshiryapsiz: `${=it.from}` → `${=it.to}`

Elektron pochtani tasdiqlash kodi:

`${=it.token_str}`

Tasdiqlash kodi yigirma to'rt soat davomida amal qiladi.

Agar siz elektron pochta manzilingizni o'zgartirish uchun ariza bermagan bo'lsangiz, iltimos, ushbu xatga e'tibor bermang va hisob xavfsizligiga e'tibor bering.
